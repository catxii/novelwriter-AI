{
  "name": "novelwriter-scnet-gateway",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev --port 8787",
    "deploy": "wrangler deploy",
    "check": "wrangler check"
  },
  "devDependencies": {
    "wrangler": "^4.41.0"
  }
}
